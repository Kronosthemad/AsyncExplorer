# AsyncExplorer

It Really explanes its self it a simple file explorer that is both small and multi threaded 
because sometimes you don't need all of the explorer just to look a file is there then close 
the whole application this is just meant to get you through the filesystem with relative speed
it doesn't have all the bells and wistles yet but it does the one thing i asked of it.